# Gitleaks false-positive suppressions — one fingerprint per line
# (format: <commit>:<file>:<rule>:<line>). Each line pins ONE exact finding in
# one historical commit; it disables no rule, so the same rule still fires on
# every other match. Add an entry only after eyeballing the finding and
# confirming it is not a secret.
#
# Verified false positives:
#
# generic-api-key on a board checklist line — "Tests: key roundtrip,
# missing/corrupt key behavior (typed error, no plaintext fallback), no secrets
# in logs" — on the session-secret-storage task. The rule matched the prose
# around the word "key"; there is no secret, just a task-tracking note under
# .task-board/, committed at fd9bbd8.
fd9bbd8ff1f2210dd5894cbe6056c821fd094942:.task-board/EPIC-260715-2ptb18_local-tdlib-source/STORY-260715-2nqkhl_authorization-and-session/TASK-260715-2odowl_session-secret-storage/progress.md:generic-api-key:21
# False positive: reviewer verdict prose in the logbook about the release workflow
# holding no hardcoded secrets — generic-api-key matched the sentence itself.
c6a309fbbaf14d1cc0a3b4efcbc53cfa3962619e:LOGBOOK.md:generic-api-key:20
# False positive: Sparkle's checked-in v1 Ed25519 public verification key. It
# is the client trust anchor and intentionally public; private seeds remain
# release-environment secrets.
bfd67bf794bfd07e1c83eb3e6f7606d85d793d93:.scripts/release/publish_sparkle.py:generic-api-key:40
