# Windows packaging and acceptance

## Description
Package/sign the host and shell, register startup/sync root, support upgrades/migrations/uninstall, and run Explorer acceptance.

## Scope
MSIX or approved alternative decision, certificates, CI artifacts, installer and diagnostics.

## Acceptance Criteria
Signed release candidate installs/upgrades/removes cleanly and passes all platform/NFR gates on support matrix.
