# Package Rust core and TDLib for Android

## Description
Integrate generated Kotlin bindings and native libraries with deterministic Gradle builds and ABI/version checks.

## Scope
Debug/release symbols, stripping, licenses, and artifact size.

## Acceptance Criteria
All selected ABIs build and load; version mismatch fails clearly; artifacts contain no credentials.
