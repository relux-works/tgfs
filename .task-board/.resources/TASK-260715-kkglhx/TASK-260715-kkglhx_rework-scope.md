TEST-ONLY REWORK per TASK-260715-kkglhx_review-02.md (product code accepted, DO NOT touch it): 1) busyBound test: accept EITHER HydrationFailure(.busy) OR transport/socket error (the refusal races the write); 2) set SO_NOSIGPIPE on the raw test socket before writing (HydrationServerTests.swift:315 area) so SIGPIPE cannot kill the process. Then loop swift test >=20x to prove deterministic green + make check, route to-review.