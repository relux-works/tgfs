REWORK per review TASK-260715-gnat2x_review-verdict.md (primitives accepted, do not redesign the remover/registrar split): the launch path must NOT auto-run destructive repair. Prefer Option A: revert CompanionMain launch to add-only DomainStartupReconcile.run() and wire DomainRepair.run() behind an explicit user action (SYNC-070 auto-reconcile vs SYNC-071 user-triggered repair). Also: guard the everything-is-a-stray case (empty desired + non-empty registered must fail closed or require explicit confirmation), add a test for that destructive branch, and fix the README/doc framing so DomainStartupReconcile is not dead code. Re-run swift test + make check.