REWORK per review TASK-260715-3akqs8_review-verdict.md. Fix all three blockers: B1) resolve the path-independence contradiction - your choice of (a) keep LTO and demote path_independent to a measured field with honest per-path/over-time semantics, (b) keep LTO with a canonical fixed build path making the claim true, or (c) drop the LTO staticlib override (path-independent today at 3a58076b); update manifest, docstrings and results doc to match reality whichever you pick; B2) --check-reproducible MUST build at two different filesystem paths so the check covers the claim; B3) gitignore .scripts/packaging/swift-consumer/.build/ and Package.resolved, remove committed .build, add ignore= to the copytree with a test. NIT: switch remap to CARGO_ENCODED_RUSTFLAGS. Re-run make check + make package + swift consumer before handoff.