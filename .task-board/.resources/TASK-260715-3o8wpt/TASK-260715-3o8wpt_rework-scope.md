REWORK ONLY the architecture check script + its evidence/docs - workspace code is accepted, do not touch crates/ product code. Per review TASK-260715-3o8wpt_review-verdict.md: D1) broaden predicate detection in .scripts/check_crate_architecture.py - fail-closed line match on (cfg|cfg_attr)[!(] plus predicate word, covering cfg(all(unix,..)), cfg(not(windows)), cfg_attr(windows,..), cfg!(target_os=..); D2) error on non-null dependency target ([target.cfg(...).dependencies]) in platform-neutral crates; extend the negative-check log with these injected forms; sync the enforced-by-check claims in the script docstring and crates/README.md. Optional non-blocking: std::os:: path scan. Re-run all gates (build/test/fmt/clippy/deny/arch-check) before handoff.