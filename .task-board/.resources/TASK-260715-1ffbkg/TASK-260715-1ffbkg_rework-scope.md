REWORK per review TASK-260715-1ffbkg_review-verdict.md (everything else is verified correct - do not touch strip-before-NFC, grapheme truncation, escape byte, digest suffixing): 1) fix fold_key - either round-trip to_uppercase().to_lowercase() (catches sigma/dotless-i/Kelvin/Angstrom, over-collides only on eszett which is the safe direction) or a true Unicode case fold; keep .nfc() re-normalization; 2) add a platform-modeled fold (e.g. Platform::fold) and re-point BOTH the property suite (resolved_siblings_never_collide) and the corpus at it so the test no longer restates the implementation; 3) pin Greek final-sigma pairs and dotless-i pairs in the fixture corpus with expected suffix outcomes. Re-run make check. Reviewer reproduction at .temp/TASK-260715-1ffbkg/sigmaprobe/.