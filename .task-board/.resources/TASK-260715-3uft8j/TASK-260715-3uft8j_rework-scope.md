REWORK per review TASK-260715-3uft8j_review.md (design decisions endorsed, do not redesign): 1) fix clear_interactions() misattribution - generation/epoch carried by CallGuard or monotonic ids instead of positional seq; add a test that clears while a call is in flight (repro attached as TASK-260715-3uft8j_repro-clear-interactions.rs); 2) assert Call::Fetch arguments (item+version+range) in the interaction-order test; 3) fix fault.rs module doc to match actual every-matching-fault-advances behavior; 4) make the full-scenario test assert what its docstring claims (or fix the docstring + results.md overstatement); 5) add fault coverage for Operation::Changes and Operation::Thumbnail; 6) tighten is_err() assertions to scripted error types, fix vacuous take().all() with a len assert, drop or repoint the duplicated version-conflict test. Re-run make check.