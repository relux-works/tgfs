REWORK the 3 confirmed majors from BUG-260720-3i74u1_review-verdict.md (control-channel design accepted, do not redesign): (1) AuthSession needs Drop -> close: on drop, stop the pump thread and release the sign-in slot scope so it is not held forever (auth.rs); (2) persist_account must not finalize the real account under only the SIGN_IN_SLOT scope guard — fix the race with concurrent probe/remove of the same account (auth.rs:919-951) via proper per-account scoping/locking; (3) remove the keychain provisioning --exec mode entirely — it is a promptless secret-exfiltration primitive with zero callers; keep the signed silent-read path. Also the should-fix: add the post-embed otool -L assertion in build_app_bundle.py. Address the minors listed in the verdict. Re-run swift test + make check + package-app. Do NOT attempt live Telegram E2E — that is the external blocker under separate human decision; leave checklist item 3 unchecked and note it. Resubmit to review.