# Implement RemoteDriveSource HTTP client

## Description
Map generated protocol into core source types with streaming sinks, pagination, cursor persistence, progress, and cancellation.

## Scope
TLS, timeouts, retry taxonomy, version negotiation, and compatibility errors.

## Acceptance Criteria
Conformance passes against fake and real service; no full-file buffering; auth/content errors map correctly.
