-- Representative schema-v24 rows are supplied by the v1 fixture followed by
-- the shipped v2..v24 migrations in migration tests. This marker is required
-- for v25's bounded live-attachment lookup coverage.
SELECT 1;
