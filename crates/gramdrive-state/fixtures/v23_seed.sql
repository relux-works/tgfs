-- Representative schema-v23 rows are supplied by the v1 fixture followed by
-- the shipped v2..v23 migrations in migration tests. This marker is required
-- for v24's durable namespace-readiness coverage.
SELECT 1;
