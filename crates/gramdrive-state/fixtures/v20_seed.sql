-- Representative schema-v20 rows are supplied by the v1 fixture followed by
-- the shipped v2..v20 migrations in migration tests. This marker is required
-- for v21's installed-database rebuild coverage.
SELECT 1;
