-- Representative schema-v21 rows are supplied by the v1 fixture followed by
-- the shipped v2..v21 migrations in migration tests. This marker is required
-- for v22's installed-database journal coverage.
SELECT 1;
