-- Representative schema-v22 rows are supplied by the v1 fixture followed by
-- the shipped v2..v22 migrations in migration tests. This marker is required
-- for v23's installed-database cache-reference index coverage.
SELECT 1;
