-- Representative schema-v4 rows are supplied by the v1 fixture followed by
-- the shipped v2/v3/v4 migrations in migration tests. This file is the
-- required source-schema fixture marker for the v5 content-progress migration.
SELECT 1;
