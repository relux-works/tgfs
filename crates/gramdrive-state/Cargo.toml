[package]
name = "gramdrive-state"
description = "GramDrive local SQLite state store and schema migrations"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
gramdrive-model = { workspace = true }
rusqlite = { workspace = true }

[dev-dependencies]
# Synthetic large-account fixtures (TASK-260715-1ceq7h); dev-only per the
# architecture rules — test support never ships in a product artifact.
gramdrive-testkit = { workspace = true }

[lints]
# Shared lint set: Cargo.toml [workspace.lints].
workspace = true
